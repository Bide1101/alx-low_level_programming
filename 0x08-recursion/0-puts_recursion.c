#include "main.h"

/**
* _puts_recursion -  prints a string.
* @s: string
*/

void _puts_recursion(char *s)
{
	int i = 0;

	if (s[i] == *s)

	for (; i < s[i]; i++)
		_putchar(s[i]);

	_putchar(10);
}
