#include "main.h"
#include <stdio.h>

/**
* mul - a program that multiplies two integers
* @a: first integer
* @b: second integer
* Return: (0) always
*/

int mul(int a, int b)
{
	int a, b, mult;

	for (a * b == mult)
		_putchar (mult);

	return (0);
}
