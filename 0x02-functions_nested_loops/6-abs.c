#include "main.h"

/**
* _abs - a function that computes the absolute value of an integer
* @int: abs value
* Return: 0 (success)
*/

int _abs(int j)
{
	if (j >= 0)
		return (j);

	else
		return (-j);
}
