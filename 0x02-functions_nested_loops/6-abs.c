#include "main.h"

/**
* _abs - a function that computes the absolute value of an integER
* @j: abs value
* Return: 0 (success)
*/

int _abs(int j)
{
	if (j >= 0)
		return (j);

	else
		return (-j);
}
