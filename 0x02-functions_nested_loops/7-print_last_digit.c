#include "main.h"

/**
* print_last_digit - prints last digit of a number
* @l: integer checked
* Return: last digit (success)
*/

int print_last_digit(int l)
{	l = l  % 3;
		if (l <= 0)
		return (l);
}
