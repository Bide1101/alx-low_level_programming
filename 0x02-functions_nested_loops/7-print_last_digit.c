#include "main.h"

/**
* print_last_digit - prints last digit of a number
* @l: integer checked
* Return: 0 (success)
*/

int print_last_digit(int l)
{	int num;

		l = num % 10;

		_putchar (l);
		return (l);
}
